import type { CommonNodeType } from '@/app/components/workflow/types'

/**
 * End Node Type
 * End node is just a marker with no additional fields
 * It marks the end of the workflow
 */
export type EndNodeType = CommonNodeType
// No additional fields - End is just a marker
