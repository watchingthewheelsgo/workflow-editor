import type { ReactNode } from 'react'

export default function WorkflowEditorLayout({
  children,
}: {
  children: ReactNode
}) {
  // Simplified layout without backend-dependent providers
  return children
}
